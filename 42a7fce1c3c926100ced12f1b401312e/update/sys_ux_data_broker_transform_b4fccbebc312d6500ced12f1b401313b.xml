<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_ux_data_broker_transform">
    <sys_ux_data_broker_transform action="INSERT_OR_UPDATE">
        <acl_failure_result/>
        <api_name>global.SAM current KPI</api_name>
        <batch_exclude>false</batch_exclude>
        <description/>
        <mutates_server_data>false</mutates_server_data>
        <name>SAM current KPI</name>
        <output_schema/>
        <private>false</private>
        <props/>
        <required_translations>[ ]</required_translations>
        <schema_version>1.0.0</schema_version>
        <script><![CDATA[function transform(input) {
    var kpiProperties = [
        'sam.current.entitlement.kpi',
        'sam.current.contract.kpi',
        'sam.current.installation.kpi',
        'sam.current.subscription.kpi',
        'sam.current.discoverymodel.kpi',
        'sam.current.softwaremodel.kpi'
    ];

    var kpiValues = {
        'entitlement_kpi': [],
        'contract_kpi': [],
        'installation_kpi': [],
        'subscription_kpi': [],
        'discoverymodel_kpi': [],
        'softwaremodel_kpi': []
    };

    function fetchKPIs(propertyName) {
        var kpis = [];
        var propertyValue = gs.getProperty(propertyName);

        if (propertyValue) {
            try {
                gs.log("Raw property value: " + propertyValue);
                kpis = JSON.parse(propertyValue);
                gs.log("Parsed KPIs for " + propertyName + ": " + JSON.stringify(kpis));
            } catch (e) {
                gs.log("Error parsing " + propertyName + ": " + e.message);
            }
        } else {
            gs.log("Property " + propertyName + " is not set or is empty.");
        }
        return kpis;
    }

    function processKPI(kpis, kpiType) {
        if (Array.isArray(kpis)) {
            gs.log("Processing KPIs for " + kpiType + ": " + JSON.stringify(kpis));
            kpis.forEach(function(kpi, index) {
                if (kpi.table && kpi.field) {
                    var queryCount = getqueryCount(kpi.table, kpi.field);
                    var totalCount = getTotalCount(kpi.table);
                    var percentage = totalCount > 0 ? Math.round((queryCount / totalCount) * 100) : 0;

                    kpiValues[kpiType].push({
                        'kpi': kpi.kpi,
                        'description': kpi.description,
                        'table': kpi.table,
                        'query_count': queryCount,
                        'total': totalCount,
                        'percentage': percentage, // Rounded to 2 decimal places
                        'action': kpi.action,
						"recommendation": kpi.recommendation
                    });

                    gs.log("Processed KPI: " + JSON.stringify(kpiValues[kpiType][kpiValues[kpiType].length - 1]));
                } else {
                    gs.log("Skipping KPI missing table or query: " + JSON.stringify(kpi));
                }
            });
        } else {
            gs.log("Invalid KPI format for " + kpiType + ": " + JSON.stringify(kpis));
        }
    }

    function getqueryCount(tableName, field) {
        try {
            var gr = new GlideAggregate(tableName);
            gr.addEncodedQuery(field);
            gr.addAggregate('COUNT');
            gr.query();

            if (gr.next()) {
                return parseInt(gr.getAggregate('COUNT'), 10);
            }
            return 0;
        } catch (error) {
            gs.log("Error querying table " + tableName + ": " + error.message);
            return 0;
        }
    }

    function getTotalCount(tableName) {
        try {
            var gr = new GlideAggregate(tableName);
            gr.addAggregate('COUNT');
            gr.query();

            if (gr.next()) {
                return parseInt(gr.getAggregate('COUNT'), 10);
            }
            return 0;
        } catch (error) {
            gs.log("Error querying total count for table " + tableName + ": " + error.message);
            return 0;
        }
    }

    kpiProperties.forEach(function(property, index) {
        var kpiType = Object.keys(kpiValues)[index];
        gs.log("Fetching KPIs for property: " + property + " as type: " + kpiType);
        var kpis = fetchKPIs(property);
        processKPI(kpis, kpiType);
    });

    return kpiValues;
}
]]></script>
        <sys_class_name>sys_ux_data_broker_transform</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2024-12-12 08:59:58</sys_created_on>
        <sys_id>b4fccbebc312d6500ced12f1b401313b</sys_id>
        <sys_mod_count>28</sys_mod_count>
        <sys_name>SAM current KPI</sys_name>
        <sys_package display_value="Global" source="global">global</sys_package>
        <sys_policy/>
        <sys_scope display_value="SAMFramework">42a7fce1c3c926100ced12f1b401312e</sys_scope>
        <sys_update_name>sys_ux_data_broker_transform_b4fccbebc312d6500ced12f1b401313b</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2025-04-21 14:00:26</sys_updated_on>
    </sys_ux_data_broker_transform>
    <sys_translated_text action="delete_multiple" query="documentkey=b4fccbebc312d6500ced12f1b401313b"/>
    <sys_es_latest_script action="INSERT_OR_UPDATE">
        <id>b4fccbebc312d6500ced12f1b401313b</id>
        <sys_created_by>Arul_123</sys_created_by>
        <sys_created_on>2025-01-22 05:36:49</sys_created_on>
        <sys_id>88f865b4c3131a100ced12f1b40131d6</sys_id>
        <sys_mod_count>0</sys_mod_count>
        <sys_updated_by>Arul_123</sys_updated_by>
        <sys_updated_on>2025-01-22 05:36:49</sys_updated_on>
        <table>sys_ux_data_broker_transform</table>
        <use_es_latest>false</use_es_latest>
    </sys_es_latest_script>
</record_update>
