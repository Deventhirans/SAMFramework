<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_ux_data_broker_transform">
    <sys_ux_data_broker_transform action="INSERT_OR_UPDATE">
        <acl_failure_result/>
        <api_name>global.user single</api_name>
        <batch_exclude>false</batch_exclude>
        <description/>
        <mutates_server_data>false</mutates_server_data>
        <name>user single</name>
        <output_schema/>
        <private>false</private>
        <props/>
        <required_translations>[ ]</required_translations>
        <schema_version>1.0.0</schema_version>
        <script><![CDATA[function transform(input) {
    var sysPropertyName = "sam.foundation.current.user.kpi";
    var totalKpiCount = 0;
    var totalRecords = 0;

    // Get system property value
    var sysPropertyValue = gs.getProperty(sysPropertyName);

    if (!sysPropertyValue || sysPropertyValue.trim() === "") {
        gs.error("Invalid or missing system property: " + sysPropertyName);
        return { output: "Error: Invalid configuration" };
    }

    try {
        var config = JSON.parse(sysPropertyValue);
        var kpisArray = Array.isArray(config) ? config : config.kpis;

        // Get total record count (either from config or default "cmn_location")
        var totalRecordsTable = config.totalRecordsTable || "cmn_location";
        gs.log("Fetching total records from table: " + totalRecordsTable);

        var recordCount = new GlideAggregate(totalRecordsTable);
        recordCount.addAggregate('COUNT');
        recordCount.query();

        if (recordCount.next()) {
            totalRecords = parseInt(recordCount.getAggregate('COUNT'), 10);
            gs.log("Total records found: " + totalRecords);
        } else {
            gs.log("No records found in table: " + totalRecordsTable);
        }

        // Process KPI queries
        if (Array.isArray(kpisArray)) {
            gs.log("Processing KPI queries...");

            kpisArray.forEach(function (kpi, index) {
                gs.log("Processing KPI #" + index + " - KPI Name: " + kpi.kpi);

                if (!kpi.table) {
                    gs.log("Skipping KPI #" + index + " - Missing table name.");
                    return;
                }

                var kpiQuery = new GlideAggregate(kpi.table);
                kpiQuery.addAggregate('COUNT');

                // Apply filtering condition
                if (kpi.field && kpi.field !== 'name') {
                    gs.log("Applying condition: " + kpi.field);
                    kpiQuery.addEncodedQuery(kpi.field);
                }

                kpiQuery.query();

                while (kpiQuery.next()) {
                    var kpiCount = parseInt(kpiQuery.getAggregate('COUNT'), 10);
                    totalKpiCount += kpiCount;
                    gs.log("KPI #" + index + " - Count: " + kpiCount);
                }
            });

            gs.log("Total KPI matching records: " + totalKpiCount);
        } else {
            gs.log("No valid KPI configurations found.");
        }

        gs.log("Total record count: " + totalRecords);

        // Calculate percentage
        var percentage = (totalRecords > 0) ? (totalKpiCount / totalRecords) * 100 : 0;
        gs.log("Final KPI Percentage: " + percentage.toFixed(2) + "%");
        
        return { output: percentage.toFixed(2) + "%" };

    } catch (e) {
        gs.error("Error in KPI calculation: " + e.message);
        return { output: "Error in processing" };
    }
}

transform("!");
]]></script>
        <sys_class_name>sys_ux_data_broker_transform</sys_class_name>
        <sys_created_by>Loga_123</sys_created_by>
        <sys_created_on>2025-02-25 15:48:18</sys_created_on>
        <sys_id>76cab27fc3ff9e140ced12f1b4013184</sys_id>
        <sys_mod_count>6</sys_mod_count>
        <sys_name>user single</sys_name>
        <sys_package display_value="Global" source="global">global</sys_package>
        <sys_policy/>
        <sys_scope display_value="SAMFramework">42a7fce1c3c926100ced12f1b401312e</sys_scope>
        <sys_update_name>sys_ux_data_broker_transform_76cab27fc3ff9e140ced12f1b4013184</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2025-04-21 14:00:27</sys_updated_on>
    </sys_ux_data_broker_transform>
    <sys_translated_text action="delete_multiple" query="documentkey=76cab27fc3ff9e140ced12f1b4013184"/>
    <sys_es_latest_script action="INSERT_OR_UPDATE">
        <id>76cab27fc3ff9e140ced12f1b4013184</id>
        <sys_created_by>Loga_123</sys_created_by>
        <sys_created_on>2025-02-25 15:48:18</sys_created_on>
        <sys_id>f8faf27fc3ff9e140ced12f1b401310f</sys_id>
        <sys_mod_count>0</sys_mod_count>
        <sys_updated_by>Loga_123</sys_updated_by>
        <sys_updated_on>2025-02-25 15:48:18</sys_updated_on>
        <table>sys_ux_data_broker_transform</table>
        <use_es_latest>true</use_es_latest>
    </sys_es_latest_script>
</record_update>
