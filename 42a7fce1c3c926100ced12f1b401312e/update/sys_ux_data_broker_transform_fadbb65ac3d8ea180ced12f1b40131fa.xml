<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_ux_data_broker_transform">
    <sys_ux_data_broker_transform action="INSERT_OR_UPDATE">
        <acl_failure_result/>
        <api_name>global.GetWalkValueBuilderTasks</api_name>
        <batch_exclude>false</batch_exclude>
        <description/>
        <mutates_server_data>false</mutates_server_data>
        <name>GetWalkValueBuilderTasks</name>
        <output_schema/>
        <private>false</private>
        <props/>
        <required_translations>[ ]</required_translations>
        <schema_version>1.0.0</schema_version>
        <script><![CDATA[function transform(input) { 
    var kpiProperties = [
        'customwalk.properties'
    ];
    
    function fetchKPIs(propertyName) {
        var propertyValue = gs.getProperty(propertyName);
        
        if (!propertyValue) {
            gs.error("Property '" + propertyName + "' is empty or not set.");
            return null;
        }
        
        try {
            var categories = JSON.parse(propertyValue);
            var categoryArray = []; // Store as an array of arrays
            
            categories.forEach(function(category) {
                if (!category.category || !category.actions || !Array.isArray(category.actions)) {
                    gs.error("Invalid category format: " + JSON.stringify(category));
                    return;
                }

                var actionArray = []; // Store actions separately
                
                category.actions.forEach(function(action) {
                    if (action.description && action.short_description) {
                        var actionObj = {
                            type: action.type || "",
                            description: action.description.trim(),
                            short_description: action.short_description.trim(),
                            instruction: action.instruction || "",
                            value_builder_tasks: [] // Will hold related tasks
                        };

                        // Add additional fields if available
                        if (action.plugins) {
                            actionObj.plugins = action.plugins;
                        }
                        if (action.properties) {
                            actionObj.properties = action.properties;
                        }
                        
                        actionArray.push(actionObj);
                    } else {
                        gs.error("Missing required fields in action: " + JSON.stringify(action));
                    }
                });

                // Only add categories with actions
                if (actionArray.length > 0) {
                    categoryArray.push([category.category, actionArray]); // Store as array instead of object
                }
            });

            return categoryArray;
        } catch (e) {
            gs.error("Error parsing property '" + propertyName + "': " + e.message);
            return null;
        }
    }
    
    function fetchTasksFromValueBuilder(action) {
        var tasks = [];
        if (!action.description || !action.short_description) {
            gs.error("Missing description or short_description for action: " + JSON.stringify(action));
            return tasks;
        }
        
        var taskGR = new GlideRecord('samp_sp_vb_task');
        taskGR.addQuery('description', action.description);
        taskGR.addQuery('short_description', action.short_description);
        taskGR.addEncodedQuery('stateNOT IN2,-5,3,7'); // Exclude closed/canceled states
        taskGR.query();
        
        while (taskGR.next()) {
            tasks.push({
                sys_id: taskGR.getValue('sys_id'),
                number: taskGR.getValue('number'),
                description: taskGR.getValue('description'),
                short_description: taskGR.getValue('short_description'),
                state: taskGR.getDisplayValue('state'),
                assigned_to: taskGR.getDisplayValue('assigned_to')
            });
        }
        
        return tasks;
    }
    
    var result = {
        categories: []
    };
    
    for (var i = 0; i < kpiProperties.length; i++) {
        var categoryArray = fetchKPIs(kpiProperties[i]);
        
        if (categoryArray && categoryArray.length > 0) {
            // For each category, fetch the related tasks for each action
            categoryArray.forEach(function(category) {
                var categoryName = category[0]; // Extract category name
                var actions = category[1]; // Extract actions
                
                actions.forEach(function(action) {
                    action.value_builder_tasks = fetchTasksFromValueBuilder(action);
                });
                
                result.categories.push([categoryName, actions]); // Store as array
            });
        }
    }
    
    gs.info("Final KPI Data: " + JSON.stringify(result));
    
    return result;
}
]]></script>
        <sys_class_name>sys_ux_data_broker_transform</sys_class_name>
        <sys_created_by>Loga_123</sys_created_by>
        <sys_created_on>2025-03-17 20:34:18</sys_created_on>
        <sys_id>fadbb65ac3d8ea180ced12f1b40131fa</sys_id>
        <sys_mod_count>3</sys_mod_count>
        <sys_name>GetWalkValueBuilderTasks</sys_name>
        <sys_package display_value="Global" source="global">global</sys_package>
        <sys_policy/>
        <sys_scope display_value="SAMFramework">42a7fce1c3c926100ced12f1b401312e</sys_scope>
        <sys_update_name>sys_ux_data_broker_transform_fadbb65ac3d8ea180ced12f1b40131fa</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2025-04-21 14:00:27</sys_updated_on>
    </sys_ux_data_broker_transform>
    <sys_translated_text action="delete_multiple" query="documentkey=fadbb65ac3d8ea180ced12f1b40131fa"/>
    <sys_es_latest_script action="INSERT_OR_UPDATE">
        <id>fadbb65ac3d8ea180ced12f1b40131fa</id>
        <sys_created_by>Loga_123</sys_created_by>
        <sys_created_on>2025-03-17 20:34:18</sys_created_on>
        <sys_id>5d3cf29ec3d8ea180ced12f1b40131ad</sys_id>
        <sys_mod_count>0</sys_mod_count>
        <sys_updated_by>Loga_123</sys_updated_by>
        <sys_updated_on>2025-03-17 20:34:18</sys_updated_on>
        <table>sys_ux_data_broker_transform</table>
        <use_es_latest>true</use_es_latest>
    </sys_es_latest_script>
</record_update>
