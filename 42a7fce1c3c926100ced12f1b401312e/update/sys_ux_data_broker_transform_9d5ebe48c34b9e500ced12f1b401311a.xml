<?xml version="1.0" encoding="UTF-8"?><record_update table="sys_ux_data_broker_transform">
    <sys_ux_data_broker_transform action="INSERT_OR_UPDATE">
        <acl_failure_result/>
        <api_name>global.Sam_foundation_current_kpis</api_name>
        <batch_exclude>false</batch_exclude>
        <description/>
        <mutates_server_data>false</mutates_server_data>
        <name>Sam_foundation_current_kpis</name>
        <output_schema/>
        <private>false</private>
        <props/>
        <required_translations>[ ]</required_translations>
        <schema_version>1.0.0</schema_version>
        <script><![CDATA[function transform(input) {
    var kpiProperties = [
        'sam.foundation.current.users.kpi',
        'sam.foundation.current.company.kpi',
        'sam.foundation.current.location.kpi',
        'sam.foundation.current.costcenter.kpi',
        'sam.foundation.current.businessunit.kpi',
        'sam.foundation.current.department.kpi',
        'sam.current.contract.kpi',
        'sam.current.discoverymodel.kpi',
        'sam.current.entitlement.kpi',
        'sam.current.installation.kpi',
        'sam.current.softwaremodel.kpi',
        'sam.current.subscription.kpi',
        'sam.implementation.custom.data.entities'
    ];

    var kpiValues = {
        'users_kpi': [],
        'company_kpi': [],
        'location_kpi': [],
        'costcenter_kpi': [],
        'businessunit_kpi': [],
        'department_kpi': [],
        'contract_kpi': [],
        'discoverymodel_kpi': [],
        'entitlement_kpi': [],
        'installation_kpi': [],
        'softwaremodel_kpi': [],
        'subscription_kpi': [],
        'custom_data_entity': [],
    };

    function fetchKPIs(propertyName) {
        var kpis = [];
        var propertyValue = gs.getProperty(propertyName);

        if (propertyValue) {
            try {
                gs.info("Raw property value: " + propertyValue);
                kpis = JSON.parse(propertyValue);
            } catch (e) {
                gs.info("Error parsing " + propertyName + ": " + e.message);
            }
        }
        return kpis;
    }

    function getTotalCount(tableName) {
        try {
            var gr = new GlideAggregate(tableName);
            gr.addAggregate('COUNT');
            gr.query();
            return gr.next() ? gr.getAggregate('COUNT') : 0;
        } catch (error) {
            gs.info("Error counting records in table " + tableName + ": " + error.message);
            return 0;
        }
    }

    function processKPI(kpis, kpiType) {
        var totalQueryCount = 0;
        var totalCount = 0;
        var kpiCount = 0;

        if (Array.isArray(kpis)) {
            kpis.forEach(function(kpi) {
                if (kpi.kpi && kpi.kpi.toLowerCase().indexOf('duplicate') !== -1) {
                    return; // Skip this KPI
                }
                if (kpi.description && kpi.description.toLowerCase().indexOf('duplicate') !== -1) {
                    return; // Skip this KPI
                }
                if (kpi.table && kpi.field) {
                    var query = getqueryCount(kpi.table, kpi.field);
					
                    var tableCount = getTotalCount(kpi.table);
                    totalCount += tableCount;
                    totalQueryCount += query;
                    kpiCount++;

                    var percentage = tableCount > 0 ? ((query / tableCount) * 100).toFixed(2) : 0;

                    kpiValues[kpiType].push({
                        'kpi': kpi.kpi,
                        'usecase': kpi.description,
                        'table': kpi.table,
                        'query': query,
                        'average': percentage,
                        'totalCount': tableCount,
                        'action': kpi.recommendation
                    });
                }
            });
        }

        kpiValues[kpiType + '_average'] = totalCount > 0 ? ((totalQueryCount / totalCount) * 100).toFixed(2) : 0;
    }

    function getqueryCount(tableName, query) {
        try {
            var gr = new GlideAggregate(tableName);
            if (query !== "none") {
                gr.addEncodedQuery(query);
            }
            gr.addAggregate('COUNT');
            gr.query();
            return gr.next() ? gr.getAggregate('COUNT') : 0;
        } catch (error) {
            gs.info("Error querying table " + tableName + ": " + error.message);
            return 0;
        }
    }

    function getDuplicateCount(tableName, groupBy, query) {
        try {
            var gr = new GlideAggregate(tableName);
            gr.addEncodedQuery(query);
            gr.addAggregate('COUNT');
            gr.query();

            var duplicateCount = 0;
            while (gr.next()) {
                var count = parseInt(gr.getAggregate('COUNT'), 10);
                if (count > 1) {
                    duplicateCount++;
                }
            }
            return duplicateCount;
        } catch (error) {
            gs.info("Error querying table " + tableName + ": " + error.message);
            return 0;
        }
    }

    kpiProperties.forEach(function(property, index) {
        var kpiType = Object.keys(kpiValues)[index];
        var kpis = fetchKPIs(property);
        processKPI(kpis, kpiType);
    });

    return kpiValues;
}]]></script>
        <sys_class_name>sys_ux_data_broker_transform</sys_class_name>
        <sys_created_by>Loga_123</sys_created_by>
        <sys_created_on>2025-01-07 17:44:42</sys_created_on>
        <sys_id>9d5ebe48c34b9e500ced12f1b401311a</sys_id>
        <sys_mod_count>20</sys_mod_count>
        <sys_name>Sam_foundation_current_kpis</sys_name>
        <sys_package display_value="Global" source="global">global</sys_package>
        <sys_policy/>
        <sys_scope display_value="SAMFramework">42a7fce1c3c926100ced12f1b401312e</sys_scope>
        <sys_update_name>sys_ux_data_broker_transform_9d5ebe48c34b9e500ced12f1b401311a</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2025-04-23 11:02:53</sys_updated_on>
    </sys_ux_data_broker_transform>
    <sys_translated_text action="delete_multiple" query="documentkey=9d5ebe48c34b9e500ced12f1b401311a"/>
    <sys_es_latest_script action="INSERT_OR_UPDATE">
        <id>9d5ebe48c34b9e500ced12f1b401311a</id>
        <sys_created_by>Arul_123</sys_created_by>
        <sys_created_on>2025-02-11 12:17:38</sys_created_on>
        <sys_id>e27442dfc3e3da100ced12f1b40131de</sys_id>
        <sys_mod_count>1</sys_mod_count>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2025-04-23 11:02:53</sys_updated_on>
        <table>sys_ux_data_broker_transform</table>
        <use_es_latest>true</use_es_latest>
    </sys_es_latest_script>
</record_update>
